using namespace std;
void reverse(char name[],int n){
    int s=0;
    int e=n-1;
  while(s<e){
      swap(name[s++],name[e--]);
  }
}

int main()
{
    char name[10];
    cout<<"enter the name"<<endl;
    cin>>name;
   reverse(name,7);
   cout<<"your name is "<<name<<endl;
   
    return 0;
}
